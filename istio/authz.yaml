apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: backend-allow
  namespace: engineering
spec:
  action: ALLOW
  rules:
    - from:
        - source:
            principals: ["cluster.local/ns/engineering/sa/frontend"]
      to:
        - operation:
            methods: ["GET", "POST"]

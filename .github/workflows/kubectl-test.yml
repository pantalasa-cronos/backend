name: kubectl-min-version-test
on:
  push:
    branches:
      - "**"

jobs:
  kubectl_old_version:
    runs-on: cronos
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Install kubectl 1.24 (below minimum)
        run: |
          curl -LO "https://dl.k8s.io/release/v1.24.17/bin/linux/amd64/kubectl"
          chmod +x kubectl
          mv kubectl /usr/local/bin/kubectl
          kubectl version --client
      - name: Run kubectl (triggers ci-after-command collector)
        run: kubectl version --client
